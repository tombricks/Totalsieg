scripted_gui = {

	politics_scripted_gui = {
		context_type = player_context
		parent_window_token = politics_tab
		window_name = "politics_scripted_gui_window"

		properties = {
		}

		triggers = {
			factions_button_visible = {
				is_debug = yes
				has_country_flag = political_factions
			}
		}

		effects = {
			factions_button_click = {
				if = {
					limit = {
						has_country_flag = show_factions_ui
					}
					clr_country_flag = show_factions_ui
					else = {
						set_country_flag = show_factions_ui
					}
				}
			}
		}
	}
	
}

